Place the supplied Rubik's Cube recording here as `rubiks-turn.mp3`.

The app will use this recording for every move and restart it at the beginning of each turn. The current Web Audio sound remains as a fallback when the file is unavailable.